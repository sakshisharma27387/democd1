name: Simple Demo
on: [push]
jobs:
  demo-job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: Show files
        run: ls
      - name: Show file content
        run: cat textfile.txt
